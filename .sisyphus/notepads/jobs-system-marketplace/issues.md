# Issues

- Workspace typecheck currently fails on pre-existing unrelated error in `src/components/calendar/calendar-tab/index.tsx:30` (`screenWidth` unused); jobs-marketplace type/schema substeps did not introduce this failure.
