import { internal as apiInternal } from "../_generated/api";

export const googleCalendarNodeInternal = (
  apiInternal as unknown as {
    calendar: { googleCalendarNode: Record<string, unknown> };
  }
).calendar.googleCalendarNode as any;
