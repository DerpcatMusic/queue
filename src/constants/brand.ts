// Centralized source of truth for brand tokens
// Re-exports from @/theme/theme.ts for backward compatibility

export {
  BrandRadius,
  BrandSpacing,
  BrandType,
  getMapBrandPalette,
  MapBrandPalette,
  type ResolvedBrandScheme,
} from "@/theme/theme";
