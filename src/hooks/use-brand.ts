// DEPRECATED — use theme tokens from src/theme/theme.ts instead.
// This file is kept as a no-op to avoid breaking imports during migration.
// Delete once all imports are removed.
export function useBrand() {
  throw new Error("useBrand() is removed. Use theme tokens from src/theme/theme.ts instead.");
}
