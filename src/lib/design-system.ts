// Centralized source of truth for theme system
// Re-exports from @/theme/theme.ts for backward compatibility

export type { ThemeScheme } from "@/theme/theme";
export {
  AppTheme,
  BorderWidth,
  BrandRadius,
  BrandSpacing,
  BrandType,
  createTheme,
  FontFamily,
  FontSize,
  getTheme,
  getThemeColors,
  IconSize,
  LetterSpacing,
  LineHeight,
  Motion,
  Opacity,
  Radius,
  Spacing,
  ThemeColors,
  Typography,
} from "@/theme/theme";
