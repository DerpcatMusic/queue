// Centralized source of truth for theme preference
// Re-exports from @/theme/theme.ts for backward compatibility

export {
  applyThemePreference,
  loadThemePreference,
  persistThemePreference,
  setThemePreference,
  type ThemePreference,
} from "@/theme/theme";
